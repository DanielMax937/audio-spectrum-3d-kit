# audio-spectrum-3d Python Package

Install locally:

```bash
pip install -e .
```

Run:

```bash
audio-spectrum3d input.wav --out output.json --tensor-out mel.npy
```

The package depends only on NumPy and Python's standard `wave` module.
